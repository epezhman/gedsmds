syntax = "proto3";

package protos;
option go_package = "./;protos";

message BucketEventSubscription {
  string bucket_id = 1;
}

service MDSService {
  rpc SubscribeBucket (BucketEventSubscription) returns (BucketEventSubscription);
}
