syntax = "proto3";

package protos;
option go_package = "./;protos";

message Empty {

}

message Profiling {
  enum ProfilingType {
    NONE = 0;
    CPU = 1;
    MEMORY = 2;
  }
  ProfilingType profiling_type = 1;
}

message ProfilingResult {
  bytes content = 1;
}
